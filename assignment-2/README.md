Just a note about the tech stack.

This is everything we are using, to get the bottom sheet and map to work I to mess with the version a bit. Should just work
if you run npm install.


Expo SDK 54
React Native
TypeScript
React Navigation (Bottom Tabs + Native Stack)
@gorhom/bottom-sheet
react-native-maps
react-native-reanimated
react-native-gesture-handler
@expo/vector-icons